nginx:
  image: nginx:latest
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - ./build:/usr/share/nginx/html:ro
build:
  image: node:latest
  command: cd /data && npm i && npm run build
  volumes:
    - ./:/data